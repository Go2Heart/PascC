program test(input,output);  
var a,b:integer;  
    c:char;  
begin  
for a:=6 to 1+3 do  
    writeln(b);  
for a:=c to a+b do //start表达式不是integer类型  
    writeln(a);  
for b:=b to a>b do //end表达式不是integer类型  
    writeln(a+b)  
end.  
